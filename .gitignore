## .NET
bin/
obj/
*.user

## Visual Studio
.vs/

## OS
Thumbs.db
.DS_Store

## Local dev SQLite database - never ship real farmer data in the repo
LeafUpload.Web/*.db
LeafUpload.Web/*.db-shm
LeafUpload.Web/*.db-wal

## Claude Code local settings
.claude/settings.local.json

## Firebase service account credential - never commit, grants push-send access
LeafUpload.Web/firebase-service-account.json

## ML.NET Model Builder authoring artifact - not needed to build/run the app
## (only MLModel1.mlnet is loaded at runtime); keep locally, don't ship it.
LeafUpload.Infrastructure/MLModel1.zip

## Retraining pipeline outputs - large, machine-local, regenerate via training/
LeafUpload.ModelExporter/LeafUpload.ModelExporter/workspace/
LeafUpload.ModelExporter/LeafUpload.ModelExporter/*.mlnet
training/merged_dataset/
training/*.mlnet
training/workspace_full/
training/*.log
training/*.pid

